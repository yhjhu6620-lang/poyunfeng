## 一、实习期工作内容

实习期间，我主要参与消息营销相关后端业务及大模型服务网关的开发与维护。随着对公司技术体系、业务流程和项目架构的逐步熟悉，实习期间的工作可以分为四个阶段。

### 第一阶段：新人项目开发与技术体系熟悉

实习初期，我从零完成新人电商微服务项目，基于 Spring Boot、Dubbo、MyBatis 搭建用户、商品、订单及 API Gateway 等模块，实现注册登录、商品查询、下单及订单流转等核心链路，并通过 ZooKeeper 完成服务注册与发现。

在业务设计中，进一步完善 JWT 鉴权、数据脱敏、订单状态机、请求幂等、库存防超卖及失败补偿等机制；同时基于 ShardingSphere-JDBC 实践订单数据分库分表，采用 userId 分片与逻辑 Bucket 设计完成 2 库 4 表的数据路由。

通过新人项目，我逐步熟悉了 Gradle、Checkstyle、Dubbo、数据库分片等技术，以及公司的后端研发规范、服务化开发方式和基本研发流程，为后续进入实际业务项目开发打下基础。

### 第二阶段：参与基础业务需求，逐步熟悉实际项目研发流程

进入实际业务后，我首先从范围较小、业务边界相对明确的需求入手，逐步熟悉消息营销相关系统的代码结构、配置体系、实验体系以及真实业务中的研发上线流程。

这一阶段，我先后参与职业认证、多件折扣、视频审核模型接入等需求。在职业认证业务中，根据业务规则通过 Leo 配置中心增加相关控制配置，并修改对应业务判断逻辑、补充单元测试；在多件折扣需求中，完成订单列表灰度重开的版本控制和 A/B 实验分流逻辑；在视频审核模型部署过程中，配合上游调用方完成大模型网关字段调整、版本更新以及模型和调用方配置。

需求开发完成后，通过 HTJ 内部测试环境进行功能验证，并进一步在预发环境完成测试和配置检查。通过一系列基础需求，我逐步熟悉了从需求理解、代码开发、单元测试，到 HTJ 自测、预发验证、灰度发布和线上观察的完整研发流程。

### 第三阶段：承担完整业务需求，参与复杂业务逻辑与模块改造

随着对业务和项目理解的深入，我开始承担逻辑更加复杂、涉及模块更多的业务需求，工作内容也由单点功能修改逐渐扩展到业务逻辑设计、代码重构、实验配置、上下游协作以及上线验证等完整环节。

在营销业务中，我参与聚合优惠券、裂变邀约、团弹窗等需求开发，完成活动子渠道、业务字段、Leo 动态配置以及 A/B 实验相关逻辑，并针对部分实验分桶情况通过多组 A/A 实验进行验证。在团弹窗多轮迭代中，还负责梳理实验字段、配置白名单、埋点、监控地址以及上下游协作事项，并参与灰度发布、实验放量和上线后的监控观察。

同时，我开始参与已有业务代码的结构优化。在多件折扣和省钱助手相关业务中，通过模板方法和策略模式重构部分查询逻辑，并重新梳理 Leo 一、二级资源配置，使 Banner 等业务资源能够通过配置中心动态加载。后续在职业标签与商品类目匹配需求中，接入算法服务并重构原有匹配逻辑，协同上下游完成算法所需字段的对齐与透传，同时通过 Leo 配置中心控制新旧逻辑切换和灰度发布，降低线上变更风险。

### 第四阶段：参与稳定性治理与线上问题分析

实习后期，我的工作逐渐从业务功能开发延伸到稳定性治理和线上问题处理，并开始更多参与线上监控、资源隔离以及复杂问题定位。

在业务侧，参与团弹窗等需求上线后的灰度放量和监控观察，并通过 Trace 链路排查职业认证“无活动资格”等问题，最终定位到 Leo 配置异常并协助完成修复。

在 Aegean 大模型服务网关中，我参与完善模型调用链路的监控与稳定性治理。一方面，通过 CAT 对不同 ProxyScene 进行打点，并统一同步请求与流式请求的监控逻辑，提升模型调用耗时和异常问题的定位效率；另一方面，针对模型动态上下线场景，优化 Leo 配置热加载异常处理以及 HTTP Client 并发初始化逻辑，通过 ReentrantLock、双重检查和快速失败机制减少重复初始化和线程堆积风险。

同时，针对评价返现等上游业务调用 Aegean 网关的场景，在 Dubbo RPC 链路中增加独立 hostGroup，并完成金丝雀和预发验证后上线，通过不同资源组实现业务流量隔离，降低不同调用场景之间的资源相互影响。

此外，在一次网关调用超时和失败率升高问题中，我结合端到端请求耗时、线程池、限流、TCP 指标以及扩容前后的表现进行排查。在排除 Provider 限流和线程池瓶颈后，结合图片等大对象传输特点，将问题范围进一步收敛到网络传输及 Dubbo 网络线程处理等方向，为后续性能优化提供排查依据。

## 二、学习收获

通过这段实习，我对 Java 后端开发的认识从单纯掌握框架和基础技术，逐步转向对真实业务系统整体运行机制的理解。

首先，在技术能力方面，我将 Spring Boot、Dubbo、Redis、Kafka、数据库、配置中心、服务治理和监控等技术逐步应用到实际业务中，对服务调用、异步处理、配置管理、灰度发布、限流、幂等以及数据一致性等问题有了更加具体的认识。同时，通过 Aegean 网关相关工作，对大模型服务调用、HTTP 请求、流式响应以及网关稳定性治理有了进一步了解。

其次，在工程实践方面，我熟悉了从需求评审、方案设计、编码、自测、Code Review，到预发验证、灰度发布、监控观察和线上问题处理的完整研发流程。相比单纯实现功能，我开始更加关注代码可维护性、变更风险以及上线后的实际运行情况。

另外，我在问题分析方面也获得了较明显的提升。通过 Trace、CAT 以及 QPS、线程池、CPU、内存、TCP 等监控信息进行问题定位，我逐渐建立了从业务现象出发，结合应用、网络和上下游依赖逐层缩小问题范围的分析方法。

## 三、心得体会

这段实习让我认识到，真实的后端开发与学校项目最大的区别，不只是技术栈更加复杂，而是需要对**业务正确性、系统稳定性和工程质量同时负责**。

在学校项目中，通常更关注功能能否实现；而在实际业务中，一个需求从代码实现到真正上线，还需要考虑配置、实验、灰度、监控、异常处理以及上下游影响。尤其是在参与线上问题排查后，我更加认识到，很多问题并不能仅通过阅读业务代码解决，需要结合系统运行状态和完整调用链路进行分析。

同时，这段经历也让我认识到，后端工程能力不仅体现在“能不能写出代码”，更体现在能否理解业务、设计合理方案、控制变更风险，并在出现问题时快速定位和处理。后续我希望继续加强 JVM、网络、并发、分布式系统等基础能力，并将底层原理与实际业务问题结合起来，不断提升自己的工程实践和系统分析能力。

总体而言，这段实习经历让我完成了从学校项目开发思维向真实业务工程思维的重要转变，也让我更加明确了自己未来希望在 Java 后端和高并发分布式系统方向持续深入发展的目标。
