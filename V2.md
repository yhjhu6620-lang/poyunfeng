## 分布锁发券的流程
#### 实际原理（执行链路）
- 注解声明：方法标 @UserLock(uid="#request.uid", type=..., leaseTimeInMs=5000L)，声明锁的粒度、key、租约。
- AOP 环绕拦截：@Around("@annotation(...UserLock)") 拦截标注方法。
- SpEL 解析 uid：从入参解析出用户 uid（锁的最小粒度 = 用户维度）。
- uid 兜底校验：uid 为 null/0 直接抛异常，防止锁失效后业务"裸奔"执行导致重复发券资损。
- 拼接锁 key：type.keyPattern + uid，如 semporna:a:c:s:c:123456。
- 加锁并执行业务：bat2lock.run(lockKey, leaseTime, 1000ms等待, callback)——leaseTimeInMs 到期自动释放防死锁；onSuccess 回调里才 joinPoint.proceed() 执行原方法，onFailure 转成异常抛出。
- 异步结果同步化：通过 CommandResult 把 Bat2 的异步回调结果转成同步返回/抛异常。

基于对仓库中 `UserLock`、`UserLockEnum`、`UserLocker`、`ParseUtils`、`CommandResult`、`Bat2RepositoryConfig` 等真实代码的核对，整理如下技术方案文档（内容均来自实际实现，未做推测性发挥）。

---

# 用户级分布式锁组件技术方案

## 1. 背景与目标

聚合优惠券活动的发券是**写操作且有资损风险**。若同一用户并发触发多次领券请求，可能导致重复发券、超发等资损问题。查询类接口是只读操作，并发至多导致缓存击穿、多调一次下游，无需引入锁开销。

**目标**：以"用户"为最小锁粒度，为高并发写操作提供**声明式、零侵入**的分布式锁能力，保证同一用户同一业务串行化。

## 2. 方案概述

采用三层封装：

| 层次 | 组件 | 职责 |
|------|------|------|
| 声明层 | `@UserLock` 注解 + `UserLockEnum` 枚举 | 声明锁的 uid 来源、锁类型（key 格式）、租约时间 |
| 编排层 | `UserLocker`（Spring AOP 切面） | 拦截注解方法，解析参数、拼 key、加锁执行、结果同步返回 |
| 存储层 | `Bat2DistLock`（基于 Bat2/泰山 KV） | 真正的分布式锁实现，提供加锁与租约能力 |

整体思路：**注解一行接入 + AOP 环绕 + 底层分布式锁**，业务方法无需感知锁细节。

## 3. 核心组件设计

### 3.1 `@UserLock` 注解

元信息：`@Target(METHOD)`、`@Retention(RUNTIME)`、`@Inherited`。

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `uid` | String | 无 | SpEL 表达式，从入参解析用户 uid，如 `#request.uid` |
| `type` | UserLockEnum | 无 | 锁类型，决定锁 key 前缀格式 |
| `leaseTimeInMs` | long | 1000L | 租约时长（毫秒），到期自动释放防死锁 |

### 3.2 `UserLockEnum` 锁类型枚举

| 枚举 | code | keyPattern | 说明 |
|------|------|------------|------|
| `AGGREGATE_COUPON_SEND_COUPON` | 1 | `semporna:a:c:s:c:%d` | 聚合券发券锁 |

最终锁 key 由 `String.format(keyPattern, uid)` 生成，如 `semporna:a:c:s:c:123456`。

### 3.3 `UserLocker` AOP 切面（核心编排）

切入点：`@Around("@annotation(...UserLock)")`，执行流程：

1. 获取方法签名与 `@UserLock` 注解；
2. 通过 `ParseUtils` 用 SpEL 从入参解析出 uid；
3. **uid 兜底校验**：uid 为 null 或 0 直接抛 `SempornaBat2LockException`，防止锁失效后业务"裸奔"执行；
4. 拼接锁 key；
5. 构造 `CommandResult` 结果容器；
6. 构造 `DistLockCallback` 回调：`onSuccess` 中 `joinPoint.proceed()` 执行业务，`onFailure` 记录异常；
7. 调用 `bat2lock.run(lockKey, leaseTimeInMs, 1000L, callback)`：第 2 个参数为租约时长，第 3 个参数为等待获取锁的最长 1000ms；
8. 依据 `CommandResult` 同步返回业务结果或抛出异常。

### 3.4 `ParseUtils`（SpEL 解析）

- 使用 `DefaultParameterNameDiscoverer` 获取方法形参名（依赖编译期 `-parameters` 参数）。
- 将形参名与实际入参一一绑定到 `StandardEvaluationContext`（相当于 `#request = args[0]`）。
- 用 `SpelExpressionParser` 解析表达式并取回 `Long` 值。

### 3.5 `CommandResult`（异步转同步）

`Bat2DistLock` 是**异步回调式** API，而 AOP 切面需要同步返回业务结果，故用该"结果盒子"桥接：

- 维护 `set` 标志，`onSuccess`/`onFailure` 均只生效一次（先到先得），保证结果唯一；
- 成功后返回业务结果，失败则向上抛出 `cause`。

## 4. 存储底座：Bat2 分布式锁

- 存储引擎：Bat2（`com.pinduoduo.storageinfra.bat2`），底层为**泰山 KV**，通过长连接到 Proxy（等价于 Redis 类 KV 存储）。
- `Bat2DistLock` 由 `Bat2RepositoryConfig` 创建，依赖独立的 `distLockTableTemplate`，提供 tryLock/unlock 等锁操作，注释中标明"类似 Redis SETNX + TTL"。
- **锁表隔离**：锁使用独立的 `distLockTableName` 表，与业务缓存表隔离，原因：避免锁 key 与缓存 key 冲突、独立 TTL 策略、便于独立监控锁表健康状态。
- 工厂超时配置：单请求 500ms、pipeline 2000ms、scan 每批 200 条。

## 5. 使用示例（发券场景）

```java
@UserLock(uid = "#request.uid",
          type = UserLockEnum.AGGREGATE_COUPON_SEND_COUPON,
          leaseTimeInMs = 5000L)
public SendAggregateCouponResponse sendAggregateCoupon(SendAggregateCouponRequest request) { ... }
```

以用户 uid 为锁 key，租约 5 秒，保证同一用户发券请求串行执行，防止并发重复发放。

## 5. 可靠性保障

| 机制 | 说明 |
|------|------|
| 用户级锁粒度 | 锁 key 绑定 uid，只串行化同一用户，不误伤其他用户，并发性能可控 |
| 租约自动释放 | `leaseTimeInMs` 到期自动释放，防止业务异常/进程退出导致死锁 |
| uid 兜底校验 | uid 缺失/为 0 直接快速失败，杜绝无锁执行导致的资损 |
| 等待超时 | 获取锁最长等待 1000ms，避免长时间阻塞请求线程 |
| 失败快速返回 | 加锁失败通过异常向上抛出，不吞异常不静默降级 |
| 锁表隔离 | 独立锁表，与业务缓存解耦，便于容量与 TTL 独立管理 |

## 6. 局限与注意事项

1. SpEL 形参名解析依赖编译期 `-parameters`，缺少该参数会导致 `parameterNames` 为 null 引发 NPE。
2. `@Inherited` 对方法注解的继承语义有限，重写方法时需留意锁是否仍然生效。
3. `Bat2DistLock` 为外部库（`com.pdd.lib.bronya.distlock`），其内部锁实现细节不在本仓库，能力以 `tryLock/unlock + 租约` 语义为准。
4. 当前仅注册了发券一种锁类型，扩展新业务需在 `UserLockEnum` 中新增 key 格式。

## 7. 总结

本方案通过「声明式注解 + AOP 切面 + Bat2 分布式锁」三层设计，以一行 `@UserLock` 注解实现写操作的用户级串行化，配合 uid 兜底校验与租约自动释放，在保证资损安全的前提下实现了业务代码零侵入、锁粒度精细、接入成本极低的分布式锁能力。

---

## 线程池的设计
"我为 BatchUtils 并发编排提供一个专用线程池，具体做法是封装 Java 原生的 ThreadPoolExecutor，针对 IO 密集场景设置了几组关键参数：

① 核心线程和最大线程都设为 800——因为并发编排的任务全是下游 RPC 调用，属于 IO 密集型，线程大部分时间在等网络返回，线程数可以远超 CPU 核数，固定 800 避免线程伸缩开销； ② 队列用 SynchronousQueue——它不缓存任务，任务提交后必须有空闲线程立即接手，保证'提交即并发执行、不排队不堆积'，从而把多下游串行累加的延迟压到单次最慢任务的耗时； ③ keepAliveTime 设为 0——核心线程数等于最大线程数，线程不回收，这个参数只作占位； ④ 通过 ThreadFactoryBuilder 自定义线程名前缀 common-batchJob-pool——线程来源可识别，线上用 jstack 排查时能一眼定位是并发编排的线程池； ⑤ 拒绝策略未显式指定，用默认 AbortPolicy——线程池满了直接抛异常，配合并发任务的弱依赖降级（返回 null），实现'满了就快速失败'而不是堆积等待。

最后把这个定制好的 Executor 注入到 BatchUtils，让所有下游并发调用都跑在这套线程池上。"

面试可能追问的技术点
Q1：核心线程和最大线程为什么都是 800？（不伸缩）
设计：并发任务都是短平快的 RPC 调用，吞吐由线程数决定。固定 800 避免运行时创建/销毁线程的开销，也避免线程数波动带来的不可预测性。

Q2：为什么用 SynchronousQueue 而不是有界队列？
设计：

SynchronousQueue 不缓存任务，任务来了必须有空闲线程立即处理，否则直接拒绝。
好处是低延迟 + 不堆积：并发编排要的就是"立刻并发打出去"，排队反而增加延迟。
如果用有界队列（如 ArrayBlockingQueue），任务会先排队等待，失去并发意义，还可能积压拖垮下游。
Q3：keepAliveTime = 0 是什么意思？有意义吗？
设计：因为核心线程 = 最大线程（800=800），线程永远不会被回收，所以这个参数形同虚设，写 0 只是占位。真正的线程回收只在"核心 < 最大"时才发生。

Q4：800 个线程会不会太多？怎么定线程数？
设计：这里的任务都是 IO 密集型（RPC 网络调用），不是 CPU 密集型，线程大部分时间在等网络返回，所以线程数可以远大于 CPU 核数。800 是结合下游并发量 + 机器规格压测出来的经验值。

Q5：为什么用 ThreadFactoryBuilder 自定义线程名？
设计：common-batchJob-pool-%d 让线程可识别。排查线上问题时（jstack/日志），能一眼看出是哪个线程池在跑，可观测性要求。

Q6：满了之后的拒绝策略是什么？为什么没写？
设计：没显式写拒绝策略，用的是默认 AbortPolicy（抛异常）。并发编排允许快速失败——下游超时/线程不足时，宁可抛异常走降级（弱依赖返回 null），也不堆积等待。

一句话总结
这段线程池设计的核心思想是**"低延迟、即时执行、不堆积"**：用 SynchronousQueue 保证任务不排队直接并发，800 固定线程应对 IO 密集的 RPC 调用，自定义线程名便于排查，满了走默认拒绝策略配合降级逻辑，整体服务于 BatchUtils 的并发编排场景。
