二、CAT 监控 + Dubbo hostGroup 业务隔离
第一层：为什么隔离
你简历里说通过 hostGroup 隔离不同业务场景，具体有哪些业务需要隔离？
为什么 LLM 网关里业务隔离比普通 RPC 服务更加重要？
第二层：业务场景
3. 图片、视频、音频和文本请求，如果全部部署在同一组机器上，会产生什么问题？
4. 为什么你们会把“大对象场景”和“高保障业务”单独划分 hostGroup？
第三层：实现方式
5. Dubbo hostGroup 本质上是怎么实现流量隔离的？Consumer 怎么路由到指定的一组 Provider？
6. hostGroup 和简单地给服务扩容几台机器相比，有什么区别？
第四层：深入追问
7. hostGroup 能解决 CPU、内存、GC 之间的相互影响，但底层模型集群还是共享的怎么办？
8. 如果某个图片业务突然流量暴涨，你会通过哪些 CAT 指标判断到底是网关问题、网络问题，还是底层模型问题？
三、ReentrantLock + tryLock 实现 HTTP Client 安全懒加载
第一层：问题背景
为什么大模型 HTTP Client 不直接在 Spring 启动阶段初始化，而需要做懒加载？
原来的初始化方式具体有什么问题？为什么会导致应用启动失败？
第二层：并发问题
3. 如果多个请求第一次同时进来，同时发现 Client 没有初始化，会发生什么？
4. 你为什么选择 ReentrantLock + tryLock，而不是直接使用 synchronized？
第三层：实现细节
5. tryLock 获取锁失败的线程怎么处理？为什么不能让所有线程一直等待这把锁？
6. 初始化失败后怎么保证下一次请求还能重新初始化，而不会一直保存一个错误状态？
第四层：深入追问
7. 这个场景能不能使用 Double Check Locking？如果能，和你们当前方案相比有什么区别？
8. 如果初始化 Client 本身需要 5 秒，同时进来 1000 个请求，你这个方案怎么避免线程大量堆积？
四、流式 Base64 + Multipart 流式转发 / JVM 大对象治理
这一块我认为是你这份简历里最容易被深入追问的部分。
第一层：业务背景
为什么 LLM 网关会涉及这么严重的大文件问题？
图片目前是主要流量，那一张图片从业务方到大模型，中间经过了哪些处理？
第二层：为什么会产生内存问题
3. 原来的图片处理方式是什么？为什么 byte[] → Base64 → JSON 容易产生 JVM 大对象？
4. Base64 编码以后文件不是变小，而是大约增加 1/3，为什么还要使用 Base64？
第三层：你的优化
5. 你说改成流式 Base64，具体是怎么做的？为什么 3KB buffer 可以降低大对象分配？
6. 3KB buffer 是什么意思？是不是意味着一张 10MB 的图片 JVM 中永远只占 3KB？
7. Base64 最终还是要放进 JSON String 里，那最后不还是会产生一个很大的 String 吗？你的优化到底减少了哪部分内存？
这个第 7 题非常重要，面试官很可能会问。

第四层：Multipart
8. 为什么有些场景又改成 Multipart 流式转发，而不是 Base64？
9. Multipart 怎么做到“边从 CDN 下载，边向下游大模型上传”？这个过程中 JVM 内存占用大概是什么量级？
10. InputStream 如果不知道 Content-Length，HTTP 请求会发生什么？为什么你们需要显式处理文件长度？
最后，面试官很可能会做一次串联追问
这是判断你是不是真正理解整个系统的一组问题：
“现在假设晚上活动流量突然上涨，图片请求从 1000 QPS 涨到 4000 QPS，大模型集群 RT 也开始升高，你作为网关负责人，会按照什么顺序排查和处理？”

然后继续追：
“Sentinel、hostGroup、流式传输、CAT 这几个方案分别解决的是哪一层问题？它们之间是什么关系？”

这其实是你整个项目最核心的一句话：
Sentinel 解决流量保护，hostGroup 解决不同业务之间的资源隔离，流式传输解决单请求的大对象和 JVM 内存压力，CAT 解决整个链路的可观测性。
