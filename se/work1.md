一、聚合优惠券
工作点 1：BatchUtils 并行编排 4 路 RPC
第一层：背景
你这里为什么需要同时调用 4 路下游 RPC？分别是什么类型的依赖？
如果这 4 路 RPC 串行调用，会有什么问题？
第二层：实现
3. BatchUtils 是怎么并行编排这 4 路 RPC 的？底层使用的是线程池还是 CompletableFuture？
4. 你提到区分强弱依赖，具体是怎么定义的？强依赖失败和弱依赖失败分别怎么处理？
第三层：稳定性
5. 为什么需要统一超时？如果其中一路 RPC 一直卡住，会对整个请求产生什么影响？
6. 你这里的“快速失败”具体是什么意思？和超时有什么区别？
第四层：深入
7. 如果高峰期请求量突然翻倍，线程池任务大量堆积，你怎么处理？
8. 线程池大小应该怎么设置？为什么 IO 密集型任务不能简单按照 CPU 核数配置？
工作点 2：Redis 分布式锁 + 幂等键 + 领券记录
第一层：背景
发券场景为什么一定要做防重复？
用户连续点击两次“领取”，系统里可能发生哪些并发问题？
第二层：实现
3. Redis 分布式锁的粒度为什么选择用户级，而不是券批次级？
4. 锁的 key、TTL 是怎么设计的？如果业务执行时间超过 TTL 怎么办？
第三层：幂等
5. 已经有 Redis 分布式锁了，为什么还需要业务幂等键？
6. 如果 RPC 调用下游发券服务超时了，但下游其实已经发券成功，这时候你怎么处理？
第四层：深入
7. 分布式锁、业务幂等键、领券记录缓存三者分别解决什么问题？为什么不能只保留一个？
8. Redis 锁释放时为什么需要校验 value？不校验会有什么风险？
工作点 3：Leo 动态管理活动参数和任务超时
第一层：背景
为什么优惠券活动参数不直接写在代码或者数据库里，而要放到配置中心？
哪些参数需要动态配置？活动期间为什么经常需要调整？
第二层：实现
3. Leo 配置发生变化以后，应用怎么感知并更新本地配置？
4. 配置热更新过程中，怎么避免线程读到“更新一半”的配置？
第三层：稳定性
5. 你提到任务超时也通过 Leo 动态调整，为什么超时时间需要动态化？
6. 如果配置中心推送了一份错误配置，怎么避免线上服务直接受影响？
第四层：深入
7. 配置中心和数据库在这个场景下各自适合存什么？
8. 如果 Leo 不可用了，你们服务还能正常工作吗？
