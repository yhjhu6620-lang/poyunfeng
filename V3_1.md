下面分两部分：先给你第三节 6 个追问的「30–60 秒标准答案逐字稿」，再给你第四节三个工作点的「详细解释性表述」。

---

# 第一部分：追问标准答案（逐字稿，可直接背）

## 追问 1：为什么用乐观锁，而不是悲观锁 / 分布式锁？

> 「我先说为什么这里乐观锁是合适的：职业认证的审核、打款是**低频、短事务、冲突概率很低**的操作，如果上悲观锁或者长时间持有数据库连接，代价大、吞吐也上不去。所以我用的是 `version` 字段做乐观锁——更新时 SQL 的 WHERE 条件里同时带 `status = 期望状态` 和 `version = 旧版本`，set 的时候把 version 加一；如果 `rows = 0`，说明在我读和写之间这条记录被别人改过了，就直接抛异常回滚。这样读的时候完全不锁，只在写的时候用版本号做一次校验，冲突就失败重来。
>但乐观锁不是万能的。奖励发放是**资金操作，一旦打款重复了代价很大**，所以打款那一段我额外加了 Bat2 分布式锁把同一用户的发放串行化。总结一句话就是：**乐观锁解决「状态并发更新一致性」，分布式锁解决「资金操作防重」，两者场景不同，配合着用**。」

**为什么要这么准备**：面试官在考你「锁的选型场景意识」，不是考你背乐观锁定义。

---

## 追问 2：乐观锁失败了怎么办？会不会丢审核结果？

>乐观锁失败的本质是「版本冲突」，我的处理是**不能吞掉，要让它重试**。具体链路是：审核结果是通过消息回执异步消费进来的，乐观锁更新返回 0 行时，我会抛一个「可重试异常」，消费框架收到这个异常就会**延迟重投这条消息**（我配了最多重试 3 次，间隔是 15 秒、1 分钟、5 分钟的阶梯）。重试时因为前面的逻辑有**状态前置校验**——只有记录还在「审核中」才会往下走——所以重复投递也不会造成重复处理，只会重新尝试做一次 CAS。如果重试几次还不行，说明是异常情况，会落到死信，同时我用 Cat 打了告警点，人工介入排查。所以结论是：**乐观锁失败不会丢结果，它会被重试机制兜住，靠幂等设计保证不会重复**。

**为什么要这样能**：这是「并发 + 重试 + 幂等」三个点的综合考察，你能串起来讲，水平分直接上去。

---

## 追问 3：事务边界多大？为什么 `rollbackFor = Throwable.class`？

>我拿「提交认证申请」这个场景举例。一个申请动作，在**同一个事务**里要串 4 个写操作：插入认证记录、插入奖励记录、写「提交申请」「审核中」两条操作流水、再调用审核系统初始化审核单。这四步必须原子，任何一步失败都得整体回滚，否则会出现「有认证记录没奖励记录」或者「有记录没流水」的脏数据，后面状态机就乱了。至于为什么 `rollbackFor = Throwable.class`，因为 Spring 默认只对 RuntimeException 回滚，受检异常和 Error 不回滚；我这里希望**任何异常都回滚**，所以显式指定成 Throwable，保证资金相关的写操作不会落一半。事务边界我控制在「一次业务动作内」，绝不把 RPC 调用这种长耗时的东西包进长事务里拖住连接。

**为什么要这样能**：考你「事务边界 + 默认回滚策略」的认知，你点出「默认不回滚受检异常」这个细节是加分项。

---

## 追问 4：Kafka 消息重复消费怎么办？（幂等）

>我靠「状态机前置校验 + 乐观锁」两层来做幂等。举例说打款回执这条消息：消费进来第一步先查奖励记录，**只有状态是「提现中」才会往下处理**，如果已经是「提现成功」或者「提现失败」，说明是重复消息，直接 return 不处理。就算两条消息真的同时进来，最终更新时也是走 version 乐观锁，只有一条能更新成功，另一条 rows=0 直接抛可重试异常。审核回执同理，只有记录在「审核中」才处理。奖励发放那还多一层：先判断是不是已经「提现成功/失败」，是就直接跳过，避免重复打款。所以**幂等的核心不是靠消息去重，而是靠业务状态机本身**——消息可以重复，但状态流转是单向、不可逆的。

**为什么要这样能**：面试官想确认你不是只会背「幂等」，而是真懂「为什么状态机天然幂等」。

---

## 追问 5：你说的大模型机审，具体怎么做？

>我分两层说。第一层是**识别**：用户上传的资质图片（比如执业证书），我是调用 LLM 服务做 OCR，把图片文字识别出来，返回的是大模型的结构化文本。第二层是**判定**：拿到识别文本后，我做的机审其实是**「AI 生成痕迹检测」**——如果文本里出现了「豆包」或者「AI 生成」这类大模型水印字样，就判定这张凭证很可能是 AI 伪造的，直接标为机审不通过。为什么要做这个：因为平台上线后出现了一批用 AI 生成假资质来骗奖励的用户。所以我这个「机审」严格说是「用大模型做 OCR + 水印识别」，**不是做语义级的真伪判断**。另外针对「博主」这类职业，我们配了豁免名单，跳过 AI 检测，这个名单也是动态配置的。

**为什么要这样能**：这是最容易露怯的一条。把「我做了什么」和「我没做什么」边界讲清楚，面试官反而会觉得你诚实、有分寸。

---

## 追问 6：Leo 动态开关怎么生效？改配置要重启吗？

>不用重启。Leo 是我们的动态配置中心，我这边用 `LeoListenerManager` 注册了一个监听器，绑定到一个配置 key。运营在 Leo 后台改这个配置（比如机审开关 `enabled`、或者跳过 AI 检测的职业名单），配置一变更，**监听器回调就会触发，我把新的 JSON 反序列化成内存里的 config 对象，实时覆盖旧值**。这样线上出问题的时候，比如发现机审误杀了一批用户，我不用发版、不用重启，直接在 Leo 上把开关关掉或者把某个职业加进豁免名单，几秒内就生效了。这个能力对机审这种「规则要随时能收能放」的场景特别重要。

**为什么要这样能**：考你「配置中心热更新」原理 + 「为什么需要动态开关」的工程意识。

---

# 第二部分：第四节三个工作点的详细解释性表述

> 下面是每个工作点的「定位 + 可直接展开的完整表述 + 若被深挖还能往下讲什么」。面试时先用「定位」起头，再按需展开。

---

## 工作点 1：认证审核与奖励发放核心链路（事务 + 乐观锁 + 分布式锁 + 双状态机）

**一句话定位**：这条讲的是「资金与状态一致性的工程保障」，是我这段经历里最硬、最经得起追问的部分。

**完整表述（约 1 分钟）：**

>职业认证的完整生命周期是这样的：用户提交申请 → 认证记录和奖励记录同时创建 → 进入审核（机审 + 人审）→ 审核通过后奖励进入「可提现」→ 用户提现 → 打款成功/失败。我负责的核心链路，难点在于**状态流转的一致性**：一条认证记录关联一张奖励记录，两边状态必须同步推进，任何一个状态跳错了，要么用户拿不到钱，要么重复打款。
>
>我做的第一件事是**双状态机**：认证侧是「审核中 → 通过 / 驳回 / 机审失败」，机审失败还能申诉回退；奖励侧是「审核中 → 可提现 → 提现中 → 成功 / 失败」。所有流转都是单向、带前置校验的。
>
>第二件事是**一致性保障，三层**：① 底层用 MySQL 事务，把认证记录、奖励记录、操作流水在一个事务里原子提交；② 用 `version` 乐观锁做并发仲裁，更新带状态和版本号双重前置条件；③ 打款这段资金操作再叠加分布式锁，把同一用户并发打款串行化。这样既保证不丢、不错、不重复。

**若被追问设计细节，可继续往下讲：**
- 「为什么认证和奖励要拆两张表，不合并？」——因为奖励有独立的提现生命周期（金额、打款单号、风控事件 ID、扣款渠道），和认证的审核生命周期解耦，各走各的状态机。
- 「操作流水表是干什么的？」——记录「提交申请、审核中、通过、驳回、申诉」的完整时间线，用来做前端时间轴展示和排查，且和业务状态在同一事务里写，保证流水不丢。
- 「怎么防止状态跳级（比如直接通过跳到打款成功）？」——每次更新前先查当前状态做前置校验，再叠加 version 前置条件，非目标状态直接拦截。

---

## 工作点 2：职业认证机审链路（LLM OCR + AI 生成识别 + 人审流转 + Leo 动态开关）

**一句话定位**：这条讲的是「把 AI 能力和审核流程、动态开关串起来」的工程落地能力。

**完整表述（约 1 分钟）：**
>职业认证需要用户上传资质材料（比如医师执照）。这块的链路是：用户上传图片 → **LLM OCR 识别** → **机审判定** → 提交到审核系统走人审 → 审核结果异步回执回来，推动状态机流转。
>
>机审这里我做了一个很具体的机制：因为上线后发现有用户用 **AI 生成的假凭证**骗奖励，所以我除了用 LLM 做 OCR 之外，还加了一层「AI 生成痕迹检测」——识别出来的文本里如果出现「豆包」「AI 生成」这类大模型水印，就直接判机审不通过。同时我还做了**按职业的豁免名单**，比如「博主」这类职业不适合查 AI 生成，就配成跳过。
>
>最关键的是**这些规则不能写死在代码里**，因为机审规则要随时能收能放。所以我通过 Leo 动态配置中心来管：机审开关、跳过名单都是配置项，配置一改内存里热生效，不用重启。有一次线上机审误杀了一批用户，我直接关开关 + 加名单，几秒就止损了。

**若被追问时，可继续往下讲：**
- 「机审失败后用户怎么办？」——可以走「申诉」，记录状态从机审失败回退到审核中，重新进人审，配合乐观锁和事务保证回退一致。
- 「OCR 是同步还是异步？」——异步化处理，OCR 结果通过回执提交，不阻塞用户的提交动作。

---

## 工作点 3：Tiger（Kafka）异步 + 失败重试 + 幂等

**一句话定位**：这条讲的是「消息驱动的可靠性」——异步、重试、幂等三个关键词每个都能讲到具体代码。

**完整表述（约 1 分钟）：**
>审核结果和打款结果都是外部系统异步回传的，我这边用 Tiger（公司内部基于 Kafka 的消息队列）来消费。我做了三个层面的可靠性设计：
>
>第一是**异步解耦**：审核回执、打款回执各走各的 topic，消费端拿到消息后异步推动状态机流转，不占用主流程。
>
>第二是**失败重试**：消费失败会抛「可重试异常」，框架按「15 秒、1 分钟、5 分钟」阶梯重投，最多重试 3 次；奖励发放这块还有一套自定义的重试框架，专门处理打款这种「可能瞬时失败」的动作。
>
>第三是**幂等**，也是最重要的：重复消息进来，我先做**状态前置校验**——审核回执只有在「审核中」才处理，打款回执只有在「提现中」才处理，已经是终态的直接返回；真正更新时再靠乐观锁保证只有一个能成功。这样「消息重复 + 并发」都不会造成重复处理、重复打款。

**若被追问时，可继续往下讲：**
- 「为什么幂等核心不是靠消息去重？」→ 因为消息去重表本身有成本、还要处理事务，而业务状态机天然单向不可逆，用它做前置校验既简单又可靠。
- 「重试也会失败怎么办？」→ 超过重试次数落到死信，靠监控告警 + 人工介入，同时对账记录兜底。

---

**最后给你的一个小提醒**：这三条工作点，面试时建议**工作点 1 打头**（最硬、最能体现工程功底），**工作点 2 讲清楚边界**（机审具体做了什么/没做什么），**工作点 3 收尾**（可靠性能力）。顺序上「先一致性、再流程、再可靠性」是最自然的叙事线。
