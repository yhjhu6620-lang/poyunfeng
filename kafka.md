Kafka
MQ全称叫做“ Message Queue”，消息队列，是一种“跨进程的通信方式，用以传递上下游消息的”。
分别包括了生产者（发送方）与消费者（接收方）；MQ扮演着“中间人”的角色，消息被保存在队列中，直到它们被应用程序处理完毕。
在系统开发中，引入MQ通常是为了解决三大核心问题：解耦、异步、削峰。
1. 业务背景一：系统解耦（降低系统间的依赖风险）
- 痛点：系统A调用系统B，如果系统B宕机或升级，系统A也会跟着报错，甚至导致整个业务链路崩溃。
- 实际场景（电商下单）：用户在电商系统下单后，不仅需要更新订单系统，还需要通知库存系统扣减库存、通知物流系统发货、通知积分系统加分。
- MQ的运用：订单系统不需要直接调用其他三个系统，只需把“订单创建成功”的消息发给MQ。库存、物流、积分系统各自去订阅MQ的消息。如果积分系统暂时宕机，也不影响用户下单，订单系统完全无感知。等积分系统恢复后，再去MQ里消费未处理的消息即可。
2. 业务背景二：异步处理（提升系统响应速度）
- 痛点：某些非核心业务流程耗时较长，如果让用户同步等待，会导致页面卡顿，体验极差。
- 实际场景（用户注册）：用户注册账号，需要写入数据库（50ms），发送注册短信（200ms），发送欢迎邮件（300ms）。如果是同步执行，用户需要等待 550ms 才能看到“注册成功”。
- MQ的运用：用户注册时，系统只需写入数据库（50ms），然后向MQ发送一条“新用户注册”的消息，立即给用户返回“注册成功”（总耗时仅50ms）。后台的短信服务和邮件服务异步地从MQ获取消息并慢慢发送。用户体验大幅提升。
3. 业务背景三：流量削峰（保护高并发下的系统不被冲垮）
- 痛点：秒杀或大促活动中，瞬时流量极高，如果请求直接打到数据库，数据库会瞬间崩溃。
- 实际场景（秒杀抢购）：假设秒杀商品只有100件，但瞬间涌入了1万个请求。如果直接访问数据库，数据库扛不住并发会宕机。
- MQ的运用：把这1万个请求全部放入MQ中排队。后端的订单系统根据自身的最大处理能力（比如每秒处理100个请求），平稳地从MQ中拉取请求进行处理。超出的请求会在MQ中排队，或者直接告诉用户“排队中/售罄”。这样就起到了一个“蓄水池”的作用，保护了后端系统。
