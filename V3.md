我已完整扫描了 `foreign` 这个工程里与「职业认证」相关的全部代码链路。先说结论，再逐节展开，最后给面试官视角的追问清单。

---

## 一、先明确一点：我扫描到的「职业认证」到底是什么

你的工作区里**没有看到单独粘贴的「表述」文本**，所以我以代码本身作为「表述」来分析（如果另有成文的绩效/答辩文案，可以再发我，我帮你逐句校准）。

代码里「职业认证」对应的是 **`Maldives`（马尔代夫，内部代号 = 用户职业认证服务）** 这条链路，`foreign` 工程本质是一个**对外网关（ForeignApplication = 消息营销对外网关）**，它对外暴露 4 个 Dubbo 服务，全部透传转发给下游 Maldives：

| 对外服务（foreign 自己暴露） | 方法 | 对应下游（Maldives） |
|---|---|---|
| `ProfessionCertificationQueryService` | 查职业认证入口 | `queryProfessionCertificationEntrance` |
| `ProfessionConfigVerifyService` | 校验职业配置 / 材料 OCR | `verifyProfessionInfoConfig` / `ocrCertificationMaterial` |
| `ProfessionCertificationTagQueryService` | 按场景查标签快照 / 批量查标签内容 | `queryTagSnapshotByScene` / `batchQueryTagContent` |
| `ProfessionInviteService` | 校验邀约关系 | `checkInvitationRelation` |

---

## 二、按你要求的四个维度「审一下有没有问题」

### 1. 专业性（✅ 较好，但有两处小瑕疵）

**做得对的地方：**
- **分层干净**：`contract`（协议/DTO，发布到 Maven 给别人用）→ `service`（对外实现）→ `integration`（防腐层，负责调下游）→ `common/repository/sdk/task`，依赖方向单向、模块职责单一。
- **契约与参数校验**：请求 DTO 上有 `@NotNull`、`@Size(min=1,max=10)`，Dubbo 侧开了 `validation="true"`，非法入参在进业务方法前就被拦截。
- **敏感信息保护意识**：`TagContentDTO.tagContent`（标签明文）标注了 `@JsonIgnore` + `@ToString.Exclude`，`CheckInviteRelationRequest` 的 `queryUid/targetUidList` 也标了 `@JsonIgnore`——说明作者有意防止敏感字段被 JSON 序列化、被日志 toString 打出来。这是一个**会被面试官记住**的细节。

**可以指出的瑕疵（面试被追问时会用到，提前知道是加分项）：**
- `ProfessionCertificationQueryServiceImpl` 和 `ProfessionConfigVerifyServiceImpl` 里对 `maldivesResponse` **没做 null 判断**，直接 `getHasEntrance()` / `getPass()`。对比 `ProfessionCertificationTagQueryServiceImpl` 做了 `maldivesResponse != null` 和 `CollectionUtils.isNotEmpty` 的防御，**风格不统一**——这恰恰是面试官喜欢追问“你代码里最明显的健壮性差异是什么”的点。
- `ProfessionMaterialOcrResponse` 有 `message` 字段，但 OCR 实现里只 `setValid(...)` 没有把下游 message 透传，**信息有丢失**。

### 2. 技术深度（⚠️ 中等偏浅，要“如实定位”）
代码的真实深度不在业务，而在**网关工程治理**上：
- Bronya RPC 的统一封装（`RpcUtils.getResult`）承担了超时/异常/监控，但这套框架是**团队已有的**，你在这个模块里主要是“**会用 + 正确接入**”。
- 真正可讲的深度点：**防腐层设计、DTO 双向映射、RPC 异常治理（按服务定制异常类型）、参数校验、单测的 RPC 全 Mock 隔离、脱敏**。
- 风险：如果表述里写“主导了职业认证核心逻辑/标签生成/风控判定”，面试官一追问“那标签到底怎么生成的、快照怎么落库的”就穿帮——**因为这些全在下游 Maldives，本工程没有**。

### 3. 问题复杂度（⚠️ 偏低，诚实描述）
- 单方法本质是「字段拷贝 + 列表 Stream 映射 + 透传」，复杂度是 O(n) 的模型转换，**没有**：缓存、并发、分布式事务、算法、复杂状态机。
- 复杂点集中在“**跨服务契约对齐**”：两套 DTO（foreign 自己的 vs Maldives 的）字段要一一对应，`queryTagSnapshotByScene` 里还涉及商品四层类目（catId1~4 + catId）等字段的传递。
- 所以“问题复杂度”的诚实表述应该是：**不是算法/并发复杂度，而是“网关如何在不污染对外契约的前提下，把 5 个下游域能力安全地统一成一套对外 API”的工程复杂度**。

### 4. 价值（✅ 有真实价值，但要讲对口径）
价值不在“标签业务本身”，而在于网关职责：
- **统一对外契约**：外部只依赖 `foreign-contract`（发到 Maven），下游 Maldives 内部模型随便改，不影响调用方。
- **统一治理**：超时、异常兜底、监控埋点、参数校验、脱敏全部在网关层统一收敛，业务研发不用各自重复造轮子。
- **可测性**：通过 Mock 隔离，单测不依赖真实网络。

> 一句话总结：**代码本身是“规范的、薄薄的网关/适配层”，专业性和工程意识在线，但技术深度和问题复杂度偏浅；表述时必须按“网关+防腐层+统一 RPC 治理”的定位来讲，不能讲成“我做了职业认证核心业务”。**

---

## 三、逐个小节：面试官视角下的“怎么做 + 怎么实现”

### 小节 1：职业认证入口查询（`queryProfessionCertificationEntrance`）

**面试表述（怎么做）：**
“这是给 C 端（比如个人中心）判断是否要展示‘职业认证入口’的能力。入口侧只关心三个结果：有没有入口（`hasEntrance`）、要不要展示‘未认证’标签（`displayUncertified`）、点击后跳哪个链接（`linkUrl`）。这些判断的规则不在网关层，在下游职业认证域服务里，我这边负责把请求原样、安全地透传过去，再把结果包装成我们对外统一的响应结构。”

**怎么实现：**
- 对外 DTO 继承 `ForeignBaseRequest`（uid、pddId、设备、UA、IP、反爬参数、机型等通用字段一次性定义好，所有请求复用，避免每个接口重复写字段）。
- 服务实现 `ProfessionCertificationQueryServiceImpl` 注入 `MaldivesIntegration`，把入参翻译成 Maldives 的 request，调 `maldivesIntegration.queryProfessionCertificationEntrance()`，再手动 `setHasEntrance / setDisplayUncertified / setLinkUrl` 到自己的响应对象，最后用 `BaseResponse.success(...).build()` 统一包装返回。
- **要主动交代的改进点**：这里对下游返回值没有判空，若下游返回 null 会 NPE，属于可以再补一层防御的地方。

---

### 小节 2：职业配置校验 + 材料 OCR（`ProfessionConfigVerifyService`）

**面试表述（怎么做）：**
- 用户填写职业认证资料（职业、职称、科室、单位、执业地点、执照号、公共凭证、社交账号等 18+ 个字段）时，前端提交过来做**配置合法性校验**；同时用户上传的材料图片（医师执照等）要走 **OCR 识别**判断真伪。这两个动作的判定逻辑都在下游，网关负责把“信息字段集”和“图片列表”整理好传过去，只回传一个 `pass/valid` 布尔结果。

**怎么实现：**
- `verifyProfessionInfoConfig`：把 `VerifyProfessionInfoConfigRequest` 的 18 个字段逐个 `setXxx` 到 Maldives 的 request（体现“字段级契约映射”的繁琐与准确性要求），调用后只取 `pass`。
- `professionMaterialOcr`：入参里有 `professionCertificationId`、整体 `imageUrlList`、以及**结构化材料列表 `materialOcrList`**（每项是一个 `requirementField` + 一组图片），实现里把每个 `MaterialOcrDTO` 一对一转成下游 `MaterialOcrDTO`，再调 `ocrCertificationMaterial`。
- **可被追问的点**：`ProfessionMaterialOcrResponse` 有 `message` 但没透传，为什么丢弃下游的失败原因？（这是面试官爱抓的“信息完整性”细节。）

---

### 小节 3：职业认证标签查询（核心，C端展示标签，对应你工作区名字）

**面试表述（怎么做）：**
- 这是“C端展示职业标签”的核心场景。两条接口：
  1. **按场景查标签快照**：给定一个场景（如“评价-长标题 / 评价-短标题”）和一批“用户+商品”，返回这批用户当前应该展示的“认证快照”标识（uid + 快照ID），用于商品详情/评价区展示用户职业认证标签。
  2. **批量查标签内容**：拿到快照ID后，再批量查这些主标签的**实际展示文案（明文）**，把 `mainTagId + scene + 文案` 拼成最终展示内容。
- 场景用枚举 `ProfessionCertificationTagSceneEnum` 收敛（REVIEW=评价长标题、REVIEW_SHORT=评价短标题），并提供 `isValid(code)` 做合法性判断。

**怎么实现：**
- 入参是 `uid + scene + 商品列表（uid/goodsId/goodsName/catId1~4/catId）`，其中**商品信息是给下游风控用的**（注释里写了“每个 mainTagId 关联商品信息，用于风控”），体现“展示类接口也要带上下文给下游做风控”的意识。
- 网关层用 `Stream.map().collect()` 把上游 DTO 转成下游 DTO（用 Lombok `@Builder`），调 Maldives，再把返回的 `TagSnapshotDTO / TagContentDTO` 转换回自己契约里的对象。
- **健壮性亮点（一定要讲）**：下游返回 null 或空列表时，返回 `Collections.emptyList()` 而不是 null，保证客户端拿到的一定是“非空的空列表”，避免 C端出现空指针白屏。
- **安全亮点（一定要讲）**：标签明文 `tagContent` 标了 `@JsonIgnore` 和 `@ToString.Exclude`——因为这是用户职业认证的敏感明文，不能出现在 JSON 序列化结果和日志 toString 里，是隐私合规的体现。（可以补一句：Dubbo 走 Hessian2 序列化不受 `@JsonIgnore` 影响，所以 RPC 传输内容仍在，只是拦截了 JSON 与日志两个泄露口。）

---

### 小节 4：职业认证邀约关系校验（`ProfessionInviteService`）

**面试表述（怎么做）：**
- 提供给 fOpen 平台判断两个用户之间是否存在“邀约关系”。两种方向：`sceneType=1` 邀请人查被邀请人；`sceneType=2` 被邀请人查邀请人。入参是 `queryUid` + 目标 uid 列表，返回一个布尔 `hasRelationship`。

**怎么实现：**
- 注意 `targetUidList` 上限 10（`@Size(max=10)`），是批量查询的典型限流/防刷设计。
- 实现里对 null 做了**安全的布尔归一化**：`Boolean.TRUE.equals(...)`，下游没返回关系时也不会因为三态布尔产生隐患。
- `queryUid / targetUidList` 标了 `@JsonIgnore`，防止用户ID这种隐私字段被 JSON 序列化泄露。

---

### 小节 5（支撑层，面试必问）：防腐层 + RPC 统一封装 + Dubbo 治理

**面试表述（怎么做 + 怎么实现）：**
- **防腐层（ACL）**：下游 Maldives 有自己的 contract 包，我这边有自己的 contract 包，两套 DTO **不允许混用**。中间夹一层 `MaldivesIntegration`（接口）+ `MaldivesIntegrationImpl`（实现），职责就是“**自己的模型 ↔ 下游模型的映射**”，保证下游内部字段变化不会直接污染对外 API，也保证对外 API 不会被下游模型绑架。
- **RPC 统一封装（Bronya）**：所有下游调用都走 `RpcUtils.getResult(() -> 下游接口.方法(req), "方法名", RpcTypeEnum.MALDIVES)`，这一层统一解决了：
  - **超时**：走 `BronyaRpcTimeoutException`；
  - **业务/系统异常**：走 `BronyaRpcException`，并且通过 `RpcClientConfig` 的 `@PostConstruct` 注册了**按服务定制异常工厂**（如 Michel、Brunei 有独立异常类，Maldives 用默认）；
  - **监控**：方法名 + 服务枚举（`RpcTypeEnum.MALDIVES("用户职业认证服务","maldives")`）一起作为监控埋点标识。
- **Dubbo 配置（consumer 侧 `rpc-client.xml`）**：Maldives 5 个 reference 统一 `check="false"`（启动不强依赖下游）、`validation="true"`（开启入参校验）、`timeout="500"`（毫秒级超时兜底）；provider 侧 `rpc-server-staging.xml` 把 4 个服务用 `dubbo:service` 暴露出去。
- **JDK17 模块化**：Java 17 强封装导致 Dubbo/反射需要 `--add-opens` 打开 `java.base` 相关包（build.gradle 里配置了一串）。

---

### 小节 6：测试与质量保障（面试加分项）

**面试表述：**
- 单测**完全隔离真实网络**：`ServiceBaseTest` 用 `@ActiveProfiles("test")`，`RpcClientConfig` 用 `@Profile("!test")` 保证测试环境不加载真实 RPC 客户端。
- 每个下游服务配一个 `RpcMockHelper`（如 `MaldivesMockHelper`）实现 `RpcMock` 接口，用 Mockito 把下游 Dubbo 接口打桩；测试启动时 `RpcMockHelper.resetAllRpc()` 收集容器里所有 `RpcMock` 并统一 reset。
- 覆盖两种关键分支：**正常返回 + 下游返回空列表**（验证空值防御逻辑），断言响应 `isSuccess`、list 非 null、字段值正确。

---

## 四、总结：面试官最可能追问的技术问题与深度

> 关键词：**网关/适配层、防腐层、Dubbo、RPC 治理、参数校验、脱敏、单测隔离**。

1. **架构层**：“为什么拆这么多模块？contract 为什么单独发 Maven？防腐层解决了什么问题？”——考察是否真的理解分层动机，而不是背模板。
2. **RPC 层**：“Dubbo 的 timeout、retries、check、validation 分别干什么？为什么这里 retries=0？”——写接口场景不重试、读接口才可重试；check=false 是启动容错。
3. **异常与超时**：“下游超时你怎么处理？异常怎么统一的？为什么会按服务定制异常类？”——Bronze 框架把异常归一化，便于上层统一捕获与告警。
4. **健壮性**：“下游返回 null 你怎么处理？为什么有的方法判空、有的没判？”——这正是本工程里**你能主动暴露并改进**的点（入口查询、配置校验两个方法缺少判空）。
5. **安全**：“`@JsonIgnore` 和 `@ToString.Exclude` 为什么用？Dubbo 序列化受不受它影响？”——JSON/日志脱敏 vs Hessian2 传输的差别，能讲清说明水平不浅。
6. **参数校验**：“`validation=true` + JSR-303 注解的校验发生在哪一层？被拦截的请求会怎样？”——Dubbo provider 层 filter 拦截，返回参数错误。
7. **测试**：“单测为什么能完全不连下游？Mockito 怎么隔离？空列表分支为什么重要？”——`@Profile` 开关 + `RpcMock` 容器 + 空值防御。
8. **风险追问（必须提前准备好答案）**：“标签快照什么时候生成、标签内容怎么产生、风控具体规则是什么？”——**诚实回答：这些在下游 Maldives 服务，本工程是网关透传**。如果之前参与了 Maldives，再往下展开；没参与就明确边界，避免被连环追问打穿。

---

**给你的最终建议（一句话口径）**：把你这段经历的定位表述为——**「负责（或参与）对外网关中职业认证 C端能力的能力，完成防腐层建模、Dubbo 服务对外暴露、RPC 异常/超时治理、参数校验与敏感信息脱敏，并建立不依赖下游网络的单测隔离体系」**；而不要表述成“做了职业认证核心业务逻辑/标签生成/风控”。这样定位准确、可讲、也经得起追问。

如果你手上其实还有一份成文的「职业认证表述」文本（比如晋升/答辩/绩效描述），把它发我，我可以逐句对照代码帮你**标出哪里会招来追问、哪里与代码事实不符、哪里可以再拔高**。
