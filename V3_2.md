我已经把 `maldives`（职业认证领域服务）的核心代码逐条核对完了。结论先说：**你简历这三条技术声明基本是「有代码依据的」，整体可信度很高，不是空话**。但有几处措辞需要精修，否则面试官一追问就会露怯。下面逐条给你「判定 + 代码证据 + 风险 + 建议改法」。

---

## 一、总览判定

| 简历条目 | 与代码吻合度 | 主要风险 |
|---|---|---|
| 总述：评价场景 + 标签 + 奖励 | ✅ 吻合 | 「构建职业认证能力」主语太大，需框定个人边界 |
| ① 审核+奖励链路，事务+version乐观锁 | ✅ 完全吻合 | 乐观锁只是其中一环，可再补充分布式锁更出彩 |
| ② OCR+审核系统，大模型机审/人审，Leo开关 | ✅ 吻合 | 「大模型机审」表述偏笼统，会被追问具体机制 |
| ③ Kafka异步+重试+幂等 | ⚠️ 基本吻合 | **「Kafka」要改成「Tiger（基于Kafka的消息队列）」**，这是最大的一处措辞隐患 |

---

## 二、逐条对照核实

### 第 1 条：事务 + version 乐观锁（✅ 属实，且是简历里最扎实的一条）

**代码证据：**
- `ProfessionCertificationOperateServiceImpl` 里，提交申请（`insertProfessionCertificationRecord`）、审核结果更新（`updateProfessionCertificationAuditResult`）、机审失败流转（`updateProfessionCertificationMachineAuditFailed`）、申诉（`appealProfessionCertification`）**都标了 `@Transactional(rollbackFor = Throwable.class)`**。
- 一个提交申请动作，在**同一事务**里串了 4 件事：插入认证记录、插入奖励记录、写两条操作流水（提交申请 + 审核中）、调用 Chess 审核系统初始化审核单。任何一步失败都整体回滚。
- **乐观锁是标准的 CAS 写法**：`version` 字段 + `beforeVersion + 1`，更新 SQL 的 WHERE 条件里带 `status = 期望状态 AND version = beforeVersion`，如果 `rows <= 0` 直接抛异常回滚。你代码里那句 `updateRewardRecordStatusWithRiskResult` 里专门打了 `version conflict` 日志，说明**对「版本冲突」这个并发场景是有意识的**。

**状态机（这条值得主动讲）：**
- 认证：`审核中 → 通过 / 驳回 / 机审失败`，机审失败还能走申诉回退到审核中。
- 奖励：`审核中 → 可提现 → 提现中 → 提现成功 / 提现失败`。

**措辞风险与建议：**
- 你现在只说「乐观锁」，但代码里**还有一层更强的防护你没写**：奖励发放时用了 `Bat2DistLock`（分布式锁，`SendProfessionCertificationRewardJob` 里 `bat2DistLock.run(lockKey, 5000L, ...)`），防止同一用户并发打款。这是「乐观锁 + 分布式锁 + 状态机」三保险，建议补进简历，会让这条显得更厚实。
- 一个小瑕疵（面试官可能挖）：代码里有一句 `//trick 这个applyTimeMs是为了过底层方法的valid逻辑`——说明底层更新方法有个入参校验的「trick」，被追问「为什么这样传」时，你要能解释清楚。

---

### 第 2 条：OCR + 大模型机审/人审 + Leo 开关（✅ 属实，但「大模型机审」要讲得准）

**代码证据（`ProfessionCertificationOcrServiceImpl`）：**
- OCR 不是传统 OCR，而是**调用 LLM 服务做识别**：`aegeanIntegration.llmOcr(imageUrl)` 返回 `LlmResponse`。
- 「大模型机审」的真实含义是：解析 LLM 返回的文本（`parseLlmResponse` 解出 assistant/content），然后做**AI 生成痕迹检测**——OCR 文本里出现 `豆包` 或 `ai生成` 字样就判为「违禁/机审不通过」。这里「豆包」就是字节大模型的水印，说明你们是在识别「用户上传的是 AI 生成的假凭证」。
- 人审流转：OCR 通过 → `chessIntegration.submitMaterialOcrResult` 提交到审核系统；人审结果通过消息回执回来，在 `handleProfessionCertificationAuditResult` 里再走状态机。
- Leo 动态开关：`LeoListenerManager`（Leo 动态配置中心）监听 `maldives-base.profession.certification.machine.audit.config`，热更新 `ProfessionCertificationMachineAuditConfig`，里面有 `enabled`（机审开关）和 `skipAiCheckProfessionIds`（跳过 AI 检测的职业列表，比如「博主」类职业不查 AI 生成）。这是**真实存在的动态开关，不是编的。

**措辞风险与建议：**
- 「大模型机审」这个说法，面试官极可能追问：「你说的机审，是用大模型做什么？语义判断还是什么？」。你要能答出两层：① 大模型先做 OCR 文字识别；② 再基于识别文本做「AI 生成痕迹/违禁内容」判定。**不要被问成「我用大模型做资质真假判断」**，因为代码里没有语义级真伪判断，只是水印关键词检测。
- 建议把这条改成：「接入 LLM OCR 与审核系统，完成机审（AI 生成痕迹识别）与人审流转，机审开关与豁免名单通过 Leo 动态配置」。

---

### 第 3 条：Kafka 异步 + 重试 + 幂等（⚠️ 有一个必须改的词）

**先说最大的问题：你写的是「Kafka」，但代码里是「Tiger」。**

Tiger 是内部基于 Kafka 的消息队列框架，代码里的引用是 `pdd.tiger.kafka.clients.consumer.ConsumerRecord`，消费端统一继承 `AbstractTigerConsumer`。所以：
- **底层确实是 Kafka**，但你**直接操作的不是 Kafka 原生 API，而是 Tiger**。
- 面试官如果懂行，问一句「你消费端用的 `@KafkaListener` 还是自己写的 Consumer？提交 offset 怎么处理的？」——你答不出原生 Kafka 细节就会卡壳。所以简历最好写「Tiger（基于 Kafka 的消息队列）」，这样既准确又能展示你对内部中间件的认知。

**代码证据（这条基本属实）：**
- 两个异步回执消费：
  - 审核结果回执：`PushAuditResultReceiptTask`，topic `push-audit-result-receipt-topic`；
  - 打款结果回执：`TransferResultReceiptConsumer`，topic `koln-transfer-result`。
- 失败重试：`AbstractTigerConsumer` 里 `enableConsumeRetry(true)` + `maxReconsumeTimes(3)` + 间隔 `15s,1m,5m`，异常返回 `RECONSUME_LATER`；此外还有一套**自定义重试框架** `SimpleRetryEvent/Worker/Producer/Consumer` 专门给「奖励发放」做异步重试（`SendProfessionCertificationRewardJob`）。
- 幂等（你写「通过状态校验保障消费幂等」是对的）：
  - 审核回执：只有记录状态是「审核中」才处理，否则直接 return；
  - 打款回执：只有「提现中」才处理，重复消费直接 return；还要校验打款金额是否一致；
  - 奖励发放：`WITHDRAW_SUCCESS`/`WITHDRAW_FAILED` 直接跳过「已完成打款不再打款」。
  - 这跟第 1 条的乐观锁 CAS 是配合的：状态前置校验 + version 条件更新 = 幂等。

**建议改法**：「基于 Tiger（Kafka）消息队列实现审核、打款结果异步消费与失败重试，结合状态机前置校验与乐观锁保证消费幂等，避免重复打款。」

---

## 三、面试官视角：会被追问的技术点（提前准备答案）

按你简历的三条，最可能的追问链如下，建议逐条准备：

1. **「为什么用乐观锁而不是悲观锁 / 分布式锁？」**
   → 审核、打款是低频、短事务、冲突概率低的场景，乐观锁吞吐高、不长期持有连接；配合 version 前置条件，冲突时 `rows=0` 抛异常回滚。奖励发放打款是资金操作，冲突代价高，所以额外用分布式锁串行化（结合代码里 Bat2DistLock）。

2. **「乐观锁如果失败了怎么办？会不会丢审核结果？」**
   → 回执消费里 `rows ≤ 0` 抛 `MaldivesRetryException`，触发 Tiger 重投（3 次），配合状态前置校验保证幂等；超过次数走死信，用 Cat 打点告警人工介入。

3. **「事务边界多大？为什么 `rollbackFor = Throwable.class`？」**
   → 认证记录 + 奖励记录 + 操作流水三张表要原子一致，必须一个事务；`rollbackFor=Throwable` 保证任何异常（含运行时）都回滚，避免脏数据。

4. **「Kafka 消息重复消费怎么办？」（幂等追问）**
   → 状态机前置校验：只有「审核中」/「提现中」才处理，非目标状态直接 return；奖励发放还叠加了 `WITHDRAW_SUCCESS/FAILED` 直接跳过 + 乐观锁 version，三重防重。

5. **「你说的大模型机审，具体怎么做的？」**
   → 见上面第 2 条：LLM 做 OCR + AI 生成水印检测（豆包/ai生成），不是语义真伪判断；博主类职业通过 Leo 配置跳过 AI 检测。

6. **「Leo 动态开关是怎么生效的？改配置要重启吗？」**
   → 不重启。`LeoListenerManager` 注册监听器，配置变更时回调更新内存里的 config 对象，实时生效；机器审核开关 `enabled` 和跳过名单都是这样热更新的。

---

## 四、最终给你的简历修订建议（可直接替换）

> **职业认证（消息营销）**：面向电商评价场景建设职业认证能力，通过职业标签提升评价专业性，结合现金奖励促进用户参与。
> - 负责认证审核与奖励发放核心链路，基于 MySQL 事务 + version 乐观锁 + Bat2 分布式锁，保障审核与奖励状态在并发更新下的强一致，构建「审核中 → 通过/驳回/机审失败 → 申诉」与「可提现 → 提现中 → 成功/失败」的双状态机。
> - 负责职业认证机审链路：接入 LLM OCR 与审核系统，完成 AI 生成凭证识别（机审）与人审流转，机审开关与豁免名单通过 Leo 动态配置热生效。
> - 基于 Tiger（Kafka 消息队列）实现审核、打款结果异步消费与失败重试，通过状态机前置校验 + 乐观锁保障消费幂等，避免重复处理与重复打款。

这样改完之后，每一条都能在代码里找到对应的证据，被追问到任何一层都有底气往下答。

如果你愿意，下一步我可以帮你把**每一条背后的「追问回答逐字稿」**写出来（就是上面第三节的每一个问题，我帮你组织成 30~60 秒的标准答案），这样面试前直接背就行。
